{
  "name": "firestore-rules-tests",
  "private": true,
  "description": "Testes positivos e negativos de firestore.rules (TASK-030) via Firebase Emulator Suite + @firebase/rules-unit-testing. Roda com `firebase emulators:exec --only firestore \"npm --prefix firestore-tests test\"` a partir da raiz do repositório.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
