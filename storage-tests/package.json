{
  "name": "storage-rules-tests",
  "private": true,
  "description": "Testes positivos e negativos de storage.rules (TASK-031) via Firebase Emulator Suite + @firebase/rules-unit-testing. Roda com `firebase emulators:exec --only \"firestore,storage\" \"npm --prefix storage-tests test\"` a partir da raiz do repositório.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
